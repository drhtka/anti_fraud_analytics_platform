# Src

Здесь можно собрать код по модулям:

- `data_loader.py` - загрузка и валидация данных;
- `features.py` - генерация признаков;
- `rules.py` - rule-based антифрод-логика;
- `train.py` - обучение baseline модели;
- `inference.py` - скоринг одной транзакции или батча.
